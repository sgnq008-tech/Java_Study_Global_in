package Array;

public class PersonTest {
    public static void main(String[] args) {
        // 1. Person 객체 생성 (설계도로 실제 객체 만들기)
        Person p1 = new Person();

        // 2. p1 객체의 sayHello() 메서드 호출하기
        p1.sayHello();
    }
}